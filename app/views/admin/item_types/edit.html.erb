<h1>Editing Category</h1>

<%= form_for(@item_type,:url => admin_item_type_path(@item_type)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', admin_item_types_path %>
